import { IconCheck } from "@tabler/icons-react";

const IconPrimaryBgCheck = () => {
  return (
    <div className="rounded-full bg-primaryColor p-[6px]">
      <IconCheck size={20} />
    </div>
  );
};

export default IconPrimaryBgCheck;
